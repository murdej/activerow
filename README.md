# activerow

Annotation-driven Active Record layer for PHP 8.2+, database-agnostic via a small
swappable database bridge (a ready-made bridge for Nette Database is included).

```
composer require murdej/activerow
```

See [docs/index.md](docs/index.md) for the full reference (entity definitions, querying,
saving, events, database bridges).
